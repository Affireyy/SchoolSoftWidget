<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.education</string>
	<!--
	Sparkle auto-update feed + the public half of the EdDSA signing key.
	The matching private key never lives in this repo; CI reads it from
	the SPARKLE_PRIVATE_KEY secret at release time.

	IMPORTANT: these keys used to be set via INFOPLIST_KEY_SUFeedURL etc.
	build settings with GENERATE_INFOPLIST_FILE: YES, which never actually
	worked: Xcode's synthesis from INFOPLIST_KEY_<X> only recognizes
	Apple's own known Info.plist keys and silently drops unrecognized
	third-party ones like Sparkle's, with no warning or error. This file
	is a real, literal Info.plist (see project.yml's info: block, which
	regenerates it) so these keys are guaranteed to actually be present in
	the shipped app.
	-->
	<key>SUFeedURL</key>
	<string>https://raw.githubusercontent.com/Affireyy/SchoolSoftWidget/main/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>J1x8Iu8W4VLz4Dt8LBfB5lj230F7kW81zB9UWJz2BiY=</string>
	<key>SUEnableAutomaticChecks</key>
	<true/>
	<key>SUScheduledCheckInterval</key>
	<integer>86400</integer>
</dict>
</plist>
